const controllersWrapper = require('./controllersWrapper');
const validation = require('./validation');
const auth = require('./auth');
const upload = require('./uploadFiles');

module.exports = { controllersWrapper, validation, auth, upload };
